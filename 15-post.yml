- hosts:
  - kube-master
  - kube-worker
  - new-worker
  roles:
  - post

- hosts: kube-master[0]
  roles:
    - { role: plugins/gpu/gpu-plugin, when: "container_runtime == 'docker' and support_gpu == 'enable'" }